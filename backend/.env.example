# Environment
NODE_ENV=development

# Server
PORT=5000
API_VERSION=v1

# Database
# LOCAL Development (PostgreSQL)
DATABASE_URL="postgresql://postgres:password@localhost:5432/gearguard"

# Production/Railway (add your credentials and connection pooling parameters)
# DATABASE_URL="postgresql://user:password@host:port/database?connection_limit=10&pool_timeout=20&connect_timeout=10"

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# CORS
CORS_ORIGIN=http://localhost:3000

# Logging
LOG_LEVEL=debug
